/**
 * Postiz Launches Component
 * 
 * Integrated Postiz launches component with calendar and list views.
 * Displays scheduled posts, calendars, timelines, and post management UI.
 * 
 * Phase 5: UI Embedding - Uses PostizLaunchesEnhanced component
 */

'use client'

import PostizLaunchesEnhanced from './PostizLaunchesEnhanced'

export function PostizLaunches() {
  return <PostizLaunchesEnhanced />
}
